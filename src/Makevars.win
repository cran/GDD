# **** MODIFY THE FOLLOWING LINE BEFORE COMPILING ****
GDWIN32=N:/gdwin32

# normally you don't need to touch the following lines
PKG_CFLAGS+= -I$(GDWIN32) -DHAS_FTL
PKG_LIBS+= -lbgd -L$(GDWIN32)

# ToDo: copy $(GDWIN32)/bgd.dll into the same directory as gdd.dll
# how to do it?!?
